trainingInput:
  hyperparameters:
    goal: MINIMIZE
    hyperparameterMetricTag: average_loss
    maxTrials:
      - 2
    maxParallelTrials:
      - 2
    params:
      - parameterName: num-epochs
        type: INTEGER
        minValue: 5
        maxValue: 10
        scaleType: UNIT_LINEAR_SCALE
